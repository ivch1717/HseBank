# 🏦 HseBank  
**Выполнил:** Чичаев Иван Александрович, БПИ-249  

---

## 📋 Функциональные требования

- Управление банковскими счетами, категориями и операциями (создание, редактирование, удаление)
- Аналитика:
  - Подсчет разницы доходов и расходов за выбранный период  
  - Группировка доходов и расходов по категориям  
  - Топ самых дорогих расходов за период
- Импорт и экспорт данных в форматах CSV, JSON, YAML
- Замер времени выполнения команд
- При добавлении и удалении операции автоматически изменяется баланс счёта

---

## 📘 Применение принципов SOLID и паттернов в проекте HseBank

### SRP (Single Responsibility Principle)

Каждый компонент выполняет одну задачу:

- **Фасады** (`BankAccountFacade`, `CategoryFacade`, `OperationFacade`, `AnalyticsFacade`) - управляют бизнес-логикой и работой с репозиториями.
- **Команды** (`AddAccount`, `ImportAccountsFromCsv`, `ExportCategory` и др.) - реализуют одно конкретное действие.
- **UI (`MainWork`) и меню** - отвечают за взаимодействие с пользователем и маршрутизацию действий.
- **Импортеры** (`CsvImporter`, `JsonImporter`, `YamlImporter`) - парсят данные из соответствующих форматов.
- **Экспортеры** (`ExportToCsvVisitor`, `ExportToJsonVisitor`, `ExportToYamlVisitor`) - записывают данные в разные форматы.

Изменения в одном аспекте системы не влияют на другие части.

---

### OCP (Open/Closed Principle)

Классы закрыты для модификации, но открыты для расширения:

- Добавление нового формата импорта или экспорта не требует изменения существующего кода — достаточно создать новый `IImporter` или `IExportVisitor`.
- Команды реализуют интерфейс `ICommand`, что позволяет добавлять новые действия без изменения старых классов.
- Тип операции реализует интерфейс `ITypeOperation`, что позволяет добавлять новые виды операций.

---

### LSP (Liskov Substitution Principle)

Любая реализация интерфейса может использоваться вместо него без нарушения логики:

- Все классы, реализующие `IImporter<T>`, взаимозаменяемы (CSV, JSON, YAML).
- Все команды (`ICommand<T>`) вызываются одинаково, независимо от их назначения.
- Все фасады реализуют интерфейсы и могут быть заменены альтернативными реализациями (например, в тестах).

---

### ISP (Interface Segregation Principle)

Каждый интерфейс описывает только то, что нужно конкретному модулю:

- `ICommand<T>` - минимальный контракт для выполнения команды.
- `IImportResolver` и `IExportResolver` - отвечают только за выбор формата обработки.
- `IInputOutput` - только ввод и вывод данных.

Это исключает лишние зависимости между компонентами.

---

### DIP (Dependency Inversion Principle)

Код высокого уровня зависит от абстракций, а не от конкретных классов:

- Все зависимости передаются через конструкторы.
- `MainWork`, фасады, команды и импортеры работают с интерфейсами (`ICommandResolver`, `IImportResolver`, `IBankAccountFacade` и т. д.).
- Используется DI-контейнер `Microsoft.Extensions.DependencyInjection`.
- Реализации можно легко подменять, например, при тестировании или смене среды.

---

## 🧠 Используемые паттерны проектирования

### 1. Фабричный метод
- Каждая сущность создается через свою фабрику (`BankAccountFactory`, `CategoryFactory`, `OperationFactory`).
- Логика создания объектов сосредоточена в одном месте, что упрощает сопровождение.
- При добавлении нового типа достаточно реализовать новую фабрику.

---

### 2. Шаблонный метод
- Абстрактный класс импорта описывает общий процесс: чтение файла → парсинг → возврат данных.
- Конкретные импортеры (`CsvImporter`, `JsonImporter`, `YamlImporter`) реализуют различия в формате.
- Легко расширяется без дублирования кода.

---

### 3. Посетитель
- Экспорт данных реализован через `IExportVisitor`.
- Каждая сущность (`BankAccount`, `Category`, `Operation`) принимает посетителя и делегирует форматирование.
- Добавление нового формата экспорта не требует изменения сущностей.

---

### 4. Команда
- Каждое действие реализовано как отдельная команда (`AddAccount`, `ExportOperation`, `ImportAccountsFromCsv` и др.).
- Все команды реализуют общий интерфейс `ICommand<T>`.
- Выполнение централизовано через `CommandResolver`, обеспечивая модульность и независимость UI.

---

### 5. Фасад
- Фасады (`BankAccountFacade`, `CategoryFacade`, `OperationFacade`) предоставляют простой интерфейс к сложной логике.
- Скрывают работу с репозиториями и фабриками.
- Повышают удобство использования и изоляцию бизнес-логики.

---

### 6. Декоратор
- Для замера времени выполнения используется декоратор, оборачивающий команды.
- Позволяет добавлять функциональность (логирование, измерение времени) без изменения исходных классов.

---

### 7. Стратегия
- Применяется в `IFactoryTypeResolver` для выбора типа операции (доход или расход).
- Также используется в `ImportResolver` и `ExportResolver` для выбора формата (CSV, JSON, YAML).
- Позволяет подменять алгоритмы во время выполнения.

---

### 8. Наблюдатель
- `BankAccount` выступает как наблюдатель, а операция — как объект наблюдения.
- При создании или отмене операции счёт автоматически обновляет баланс.

---

## 🧩 Применение GRASP

| Принцип | Применение |
|----------|-------------|
| **Information Expert** | Фасады содержат бизнес-логику, а не UI |
| **Creator** | Фабрики создают объекты соответствующих сущностей |
| **Low Coupling** | Все зависимости реализованы через интерфейсы |
| **High Cohesion** | Каждый модуль решает строго одну задачу |
| **Polymorphism** | Импортеры, экспортеры и команды реализуют единые контракты |

---

## ⚙️ Инструкция по сборке и запуску

1. **Клонировать репозиторий**
   ```bash
   git clone https://github.com/ivch1717/HseBank
2. Перейти в корневую папку проекта MoscowZoo:
   ```bash
   cd HseBank
   ```

3. В корневой папке HseBank выполнить сборку:
   ```bash
   dotnet build
   ```

4. Запустить консольное приложение HseBank из корневой папки через Run.
